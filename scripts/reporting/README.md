# Reporting utilities

These scripts generate the thesis figures and experiment-specific review visuals from the archived CSV/JSON results. They were recovered during the deletion-safety audit because they were unique code files not present in the initial compact repository.

The `_legacy` Experiment 3 generator is retained only to preserve the earlier rendering variant; use the non-legacy file for the final report figures.
