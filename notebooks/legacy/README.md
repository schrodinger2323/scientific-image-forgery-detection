# Legacy notebooks

These two output-free notebooks preserve early segmentation-pipeline prototypes that contain unique historical code. They are not the recommended execution path; use the numbered notebooks in the parent directory and the reusable package under `src/` for reproduction.
